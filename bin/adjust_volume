#!/bin/bash -x

echo $1
running_sink=$(pactl list short | grep RUNNING | head -c 1)
#TODO: figure out how to not hard-code the default
first_sink=$(pactl list short | grep output | head -c 1)
sink=${running_sink:-${first_sink}}
pactl set-sink-volume ${sink} ${1}
killall -USR1 i3status
