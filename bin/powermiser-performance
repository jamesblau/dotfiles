#!/usr/bin/env bash
for attempt in $(seq 1 20); do
    nvidia-settings \
        -a "[gpu:0]/GPUPowerMizerMode=1" \
        >/dev/null 2>&1 &&
        exit 0

    sleep 0.5
done

notify-send -u critical \
    "PowerMizer" \
    "Could not enable maximum-performance mode."
